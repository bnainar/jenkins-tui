# Copy to jenkins.yaml and fill in your Jenkins host and API token.
# jenkins.yaml is git-ignored.
jenkins:
  - name: prod
    host: https://jenkins.example.com
    username: your-user
    token: your-api-token
    insecure_skip_tls_verify: false
